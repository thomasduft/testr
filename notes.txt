Todo's:

- [ ] add TestCaseManualCommand
  - testr man
  - lists a manual to the console or as MD file with available instructions of of to use the tool

- [ ] Find / Think of a way of respecting Pre-Conditions
  - reference happy case Test-Cases (e.g. to login)

----------------------------------------------------------------------------------------------------
Ideas:

- [ ] add a command that executes all TestCases found in and beneath a directory
  - TestCaseRunner
  - testr runner https://localhost:5001 -i inputDirectory -o outputDirectory ...
  - testr -tc TC-...
     - refactor run command with optional `tc|test-case` flag
       - if tc-flag given, only run desired test-case
       - if tc-flag not given, run all TestCases found beneath directory